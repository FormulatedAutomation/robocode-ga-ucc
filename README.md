# README for this task package

<Put your own content here. Do not leave as is!>

## Development

Run this with
`robo wrap --force; robo run entrypoint.cmd -v devdata\env.json`

Will automatically pause if a task fails for easier debugging